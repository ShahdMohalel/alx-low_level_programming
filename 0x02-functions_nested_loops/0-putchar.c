#include<stdio.h>
#include "main.h"
/**
 * main - putchar(sent[i])
 * void: no parameters
 * Return: 0 always
*/
int main(void)
{
sent[] = "_putchar";
for (i = 0; i < 8; i++)
{
putchar(sent[i]);
}
putchar('\n');
return (0);
}
