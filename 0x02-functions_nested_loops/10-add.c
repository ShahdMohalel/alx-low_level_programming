#include<stdio.h>
#include "main.h"
/**
 * add - adds two numbers
 * 'a':an integer number
 * 'b':an integer number
 * Return: "result" of the addition 'a'+'b'
*/
int add(int a, int b)
{
int result;
result = a + b;
return (result);
}
