#include<stdio.h>
#include "main.h"
/**
 * mul - mult two numbers.
 * @a: The parameter 'a' represents the number to be mult.
 * @b: The parameter 'b' represents the number to be mult.
 * Return: result.
*/
int mul(int a, int b)
{
int result;
result = a * b;
return (result);
}
