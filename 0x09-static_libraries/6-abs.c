#include<stdio.h>
#include "main.h"
/**
 * _abs - absolute
 * @n: the integer 'n' will be a positive number
 * Return: result
*/
int _abs(int n)
{
if (n > 0)
{
return (n);
}
else
{
return (n / -1);
}
}
